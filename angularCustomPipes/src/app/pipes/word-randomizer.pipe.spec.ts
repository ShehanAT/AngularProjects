import { WordRandomizerPipe } from './word-randomizer.pipe';

describe('WordRandomizerPipe', () => {
  it('create an instance', () => {
    const pipe = new WordRandomizerPipe();
    expect(pipe).toBeTruthy();
  });
});
